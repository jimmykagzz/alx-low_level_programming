#include <stdio.h>
#include <stdlib.h>

/**
 * main -entry point
 *
 * Return: ALways 0 (success)
 */

int main(void)
{
	int numb;

	for (numb = 0; letter <= 9; letter++)
	{
		printf(numb);
	}
	printf('\n');
	return (0);
}
